# The Clojure SDK binds the native core via the JVM Panama FFM API
# (java.lang.foreign), which is finalized in JDK 22+. Pin JDK 25 here so this
# subtree uses it even though the repo root pins temurin-21.
[tools]
java = "temurin-25.0.3+9.0.LTS"
clojure = "1.12.5.1654"
