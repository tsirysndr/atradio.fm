node_modules
dist
.turbo
*.log
.DS_Store
.env
.env.local
.env.production
coverage
.vite
.cpcache
.rebel_readline_history

# rockbox-wasm runtime, mirrored from node_modules by apps/web/vite.config.ts
apps/web/public/rockbox

# Rust (cli/ — the atradio TUI crate)
target
**/*.rs.bk

# cli/dist holds source-controlled packaging templates (deb/rpm/homebrew),
# not build output — keep it despite the `dist` rule above.
!cli/dist
!cli/dist/**
